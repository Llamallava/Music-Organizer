<Page x:Class="Music_Organizer.EditorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Music_Organizer"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditorPage">

    <Grid Margin="12" Background="DimGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="360" />
        </Grid.ColumnDefinitions>

        <!-- LEFT COLUMN -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="34"/>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Button Grid.Row="0"
        Name="Back_Button"
        Content="←"
        Width="44"
        Height="34"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        Margin="0,0,0,0"
        Click="Back_Click"
        ToolTip="Back"
        Background="#22000000"
        Foreground="White"
        BorderBrush="#33000000"
        BorderThickness="1"
        FontSize="16"
        FontWeight="SemiBold"
        Padding="0"
        Cursor="Hand"
                    >
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#33000000" />
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#44000000" />
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Album header -->
            <StackPanel Grid.Row="1"
            Margin="0,8,0,0"
            HorizontalAlignment="Center">
                <Border Width="220"
        Height="220"
        CornerRadius="10"
        ClipToBounds="True"
        HorizontalAlignment="Center">
                    <Image Source="{Binding CoverImage}"
                           Stretch="UniformToFill" />
                </Border>

                <TextBlock Text="{Binding AlbumTitle}"
           FontSize="18"
           FontWeight="SemiBold"
           Margin="0,10,0,0"
           TextWrapping="Wrap"
           TextAlignment="Center" />

                <TextBlock Text="{Binding ArtistName}"
           FontSize="13"
           Opacity="0.8"
           Margin="0,2,0,0"
           TextWrapping="Wrap"
           TextAlignment="Center" />
            </StackPanel>

            <!-- Track list -->
            <Border Grid.Row="3"
        Margin="0,8,0,0"
        BorderThickness="1"
        BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
        CornerRadius="8"
        Padding="6">

                <ListBox ItemsSource="{Binding Tabs}"
                         SelectedItem="{Binding SelectedTab, Mode=TwoWay}"
                         BorderThickness="0"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="6"
                                    Padding="8"
                                    Margin="0,0,0,6">
                                <TextBlock Text="{Binding DisplayName}"
                                           TextWrapping="Wrap" />
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Root"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="6">
                                            <ContentPresenter />
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Root" Property="Background" Value="#11000000" />
                                            </Trigger>

                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Root" Property="Background" Value="#22007ACC" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                </ListBox>
            </Border>
        </Grid>

        <!-- MIDDLE COLUMN: notes + score -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="*" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding SelectedTab.DisplayName}"
               FontSize="16"
               FontWeight="SemiBold"
               VerticalAlignment="Center" />

                <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="16,0,0,0">
                    <Button Content="Save"
                Width="90"
                Margin="0,0,8,0"
                Command="{Binding SaveCommand}" />

                    <Button Content="Save &amp; Exit"
                Width="110"
                Click="SaveExit_Click" />
                </StackPanel>
            </StackPanel>


            <TextBox Grid.Row="2"
                     Text="{Binding SelectedTab.Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     FontSize="14" />

            <StackPanel Grid.Row="4"
                 Orientation="Horizontal"
                 VerticalAlignment="Center">

                <TextBlock Text="Score:"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0" />

                <TextBox Width="70" Name="ScoreBox"
                    Text="{Binding SelectedTab.ScoreText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    IsEnabled="{Binding SelectedTab.IsScoreEnabled}" />

                <Button Content="{Binding SelectedTab.InterludeButtonText}" 
                    Margin="12,0,0,0"
                    Padding="10,2"
                    Command="{Binding ToggleInterludeCommand}"
                    IsEnabled="{Binding CanToggleInterlude}"/>
            </StackPanel>
        </Grid>

        <!-- RIGHT COLUMN: lyrics placeholder -->
        <Grid Grid.Column="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Lyrics"
                       FontSize="16"
                       FontWeight="SemiBold" />

            <TextBox Grid.Row="2"
                     Text="{Binding SelectedTab.Lyrics}"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     FontSize="13" />
        </Grid>
    </Grid>
</Page>
